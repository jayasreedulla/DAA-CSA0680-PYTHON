Write C programs for solving recurrence relations using the Master Theorem,Substitution Method, and Iteration Method will 
demonstrate how to calculate the time complexity of an example recurrence relation using the specified technique.
def iteration_method(n):
    if n == 1:
        return 1
    return 2 * iteration_method(n // 2) + n
n = 16
result = iteration_method(n)
print(f"Result of recurrence for n = {n}: {result}")
print("Time Complexity using Iteration Method: O(n log n)")
